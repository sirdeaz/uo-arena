# Godot 4 auto-generated
.godot/
/android/

# Exported builds
/build/
/export/

# export_presets.cfg IS committed. Godot ignores it by default because it can hold
# signing credentials — ours holds none, and committing it keeps builds reproducible.

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Mono / C# (only if the project ever switches)
.mono/
data_*/
*.csproj
*.sln
